{
  "name": "apex-tracker",
  "version": "1.0.0",
  "description": "Track stats fro Apex Legends",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server",
    "server": "nodemon server",
    "client": "npm run serve --prefix client",
    "dev": "concurrently \"yarn server\" \"yarn client\""
  },
  "author": "Gabriel Santiago",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.1",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "morgan": "^1.9.1",
    "node-fetch": "^3.1.1"
  },
  "devDependencies": {
    "nodemon": "^1.19.1"
  }
}
